var app  = $( this );
/*
var userLocal = null;
var loadAppUser = function() {
  console.log("funciona esto??");
  var espacio = $('.hdd-container');

      wz.config.getSubscriptionStatus(function( err, info ){

        console.log("#START# Informacion acerca premium" , info);
        if(info.currentPlan.status == 'active'){
          userLocal.premium.info = true;
        }
        userLocal.premium.actualStorage = api.tool.bytesToUnit( api.system.quota().total).split(" ", 1)[0];
        userLocal.premium.actualPrice = info.currentPlan.amount;
        userLocal.premium.extraStorage = info.currentPlan.addQuota;
        userLocal.premium.payday = info.currentPlan.created;
        userLocal.premium.card.number = "**** **** **** "+ (info.listCards[0].last4).toString();
        userLocal.premium.card.owner = info.listCards[0].name;
        userLocal.premium.card.month = info.listCards[0].exp_month;
        userLocal.premium.card.year = info.listCards[0].exp_year;
        userLocal.premium.card.passcode = info.listCards[0].cvc_check;
      });

      if(userLocal.premium.info){
        if(espacio.hasClass('free-user')){
            espacio.removeClass('free-user');
            espacio.addClass('premium-user');
        }
      }
      console.log("#START# userLocal" , userLocal);
      espacio.data("userData", userLocal);
}
loadAppUser();
*/
